# Depth First Search

Depth First Search explores vertices recursively.

The DFS implementation in AlgoKit is built on top of an internal DFS engine that is reused by higher-level algorithms.

## Complexity

Time: O(V + E)

Space: O(V)